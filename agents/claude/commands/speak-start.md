Enable voice output. From now on, after every response you write, also run this shell command with your full response text (excluding code blocks):

~/.local/bin/speak "your response text here"

If the command fails, continue without voice. Remember this is enabled until /speak-stop is used.
